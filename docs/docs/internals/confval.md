---
title: confval
---

**confval** is a standalone Rust crate for span-first configuration parsing, validation, and lowering.
It provides the generic primitives that `snakeway-conf` builds on.

It now lives in its own [repository](https://github.com/ethanhann/confval) and has its
own [documentation site](https://ethanhann.com/confval).