[[device]]
name = "structured_logging"
type = "builtin"
builtin = "structured_logging"
enabled = true

[device.config]
include_identity = true
identity_fields = ["country", "region", "asn", "device", "bot"]
